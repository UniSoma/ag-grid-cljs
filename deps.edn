{:paths ["src/main"]

 ;; TODO(spec): cljs-bean is a direct dep only in the walking skeleton.
 ;; Per the conversion-boundary decision (agd-01ky0eck96vn) the shipped library
 ;; vendors a slice of cljs-bean under ag-grid-cljs.impl.bean (EPL headers
 ;; retained, THIRD-PARTY.md) so the runtime dependency set is empty.
 :deps {cljs-bean/cljs-bean {:mvn/version "1.9.0"}}

 :aliases
 {:dev {:extra-paths ["src/dev" "src/test"]
        :extra-deps  {thheller/shadow-cljs {:mvn/version "3.4.11"}
                      ;; dev harness only: Fulcro is the reference-consumer
                      ;; bar (ticket agd-01ky0ed8766f), never a library dep
                      com.fulcrologic/fulcro {:mvn/version "3.9.5"}}}}}
